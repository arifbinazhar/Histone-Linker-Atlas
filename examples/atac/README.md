# ATAC example files

This folder is reserved for small templates or clearly labeled example outputs.
Do not place real FASTQ, BAM, bigWig, or large intermediate files here.

The GSE236992 sample sheet in `metadata/atac_samples_GSE236992.csv` is a template until
the GEO/SRA run table is downloaded and the true run accessions are filled in.
